import { CoinTestFilterPipe } from './coin-test-filter.pipe';

describe('CoinTestFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CoinTestFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
